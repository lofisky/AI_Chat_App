#Hide environmental variables and other sensitive information
.env 

#contains all the dependencies insatlled by npm, can be massive and unnecessary to include
node_modules

#log files are generated by the application during runtime and doesn't need to be tracked by git
.log

#created by windows to cache thumbnails of images in a folder, extra clutter that we don't need and can be automatically regenerated anyway
Thumbs.db

#created by windows to store custom folder settings, not needed for the project
Desktop.ini